;; ----------------
;; version control, git, etc
;; ----------------

(use-package magit
  :init
  ;;;;;;; something like this to hide untracked and stashes:
  ;;; ;(push (cons [* commitbuf] 'hide) magit-section-initial-visibility-alist)
  ()
  :bind
  ())
